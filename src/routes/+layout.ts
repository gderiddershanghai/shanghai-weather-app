// Fully static site: every route is prerendered.
export const prerender = true;
// GitHub Pages serves directories as <dir>/index.html — always use trailing slashes.
export const trailingSlash = 'always';
